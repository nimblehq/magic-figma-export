export class FileManager {
  constructor() {}

  // function variableContent(colors): string {
  //   return colors.map((colorName, value) => {
  //     colorName + ': ' + value + ';'
  //   }).join('\n');
  // }

  // function writeToFile() {
  //   // TODO: write scss file
  //   var FileSaver = require('file-saver');
  //   var blob = new Blob([variableContent()], {type: "text/plain;charset=utf-8"});
  //   FileSaver.saveAs(blob, "_variables.scss");
  // }
}
